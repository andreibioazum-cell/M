// Proton Language
func start() {
    // Ручное выделение памяти (как malloc/new)
    var health = alloc int(100) 
    
    if (*health > 50) {
        draw_rect(100, 100, 200, 200, "red")
    }

    // Обязательное освобождение (нет GC)
    free health
}
